# Fixture for tests/check.yml. Public addresses deliberately differ from
# internal_ip so a leak of the public address is visible.
all:
  children:
    control_plane:
      hosts:
        cp1:
          ansible_host: 203.0.113.1
          ansible_user: admin
          internal_ip: 10.0.9.1
    sut:
      hosts:
        sut1:
          ansible_host: 203.0.113.10
          ansible_user: admin
          internal_ip: 10.0.9.10
